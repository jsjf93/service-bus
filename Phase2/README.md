# Phase 2: Add Real Processing

- Build a simple API that accepts requests and queues them
- Build a worker service that processes queue messages
- Example: Order processing, email sending, image resizing
